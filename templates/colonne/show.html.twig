{% extends 'base.html.twig' %}

{% block title %}Colonne{% endblock %}

{% block body %}
    <h1>Colonne</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ colonne.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ colonne.name }}</td>
            </tr>
            <tr>
                <th>Position</th>
                <td>{{ colonne.position }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_colonne_index') }}">back to list</a>

    <a href="{{ path('app_colonne_edit', {'id': colonne.id}) }}">edit</a>

    {{ include('colonne/_delete_form.html.twig') }}
{% endblock %}
